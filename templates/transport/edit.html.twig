{% extends 'base.html.twig' %}

{% block title %}Modifier un transport{% endblock %}

{% block stylesheets %}
<link rel="stylesheet" href="{{ asset('css/admin.css') }}">
{% endblock %}

{% block body %}
<div class="app-container">
    {% include 'admin/sidebar.html.twig' with {'activePage': 'transport'} %}

    <main class="content form-page">
        <div class="form-panel">
            <div>
                <span class="badge-header">Modification</span>
                <h1>Modifier le transport #{{ transport.id }}</h1>
                <p>Mettez Ã  jour les informations du trajet.</p>
            </div>
        </div>

        <div class="form-wrapper">
            {{ include('transport/_form.html.twig', {'button_label': 'Enregistrer'}) }}
            <div class="form-actions">
                <a href="{{ path('app_transport') }}" class="btn btn-secondary">â† Retour Ã  la liste</a>
                {{ include('transport/_delete_form.html.twig') }}
            </div>
        </div>
    </main>
</div>
{% endblock %}

